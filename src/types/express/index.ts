import { Request } from 'express';
import { UserAttributes } from '../../models/userModel';

// declare global {
//   namespace Express {
//     interface Request {
//       user?: { email: string; role: string };
//     }
//   }
// }
