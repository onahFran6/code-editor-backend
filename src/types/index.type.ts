export type EnvironmentConfig = {
  PORT: number;
  DB_HOST: string;
  DB_USER: string;
  DB_PASS: string;
  DB_DIALECT: string;
  DB_NAME: string;
  JWT_SECRET: string;
  JWT_TOKEN_EXPIRE: string;
};
